# Xcode build artifacts
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# Xcode Previews
*.previewinterface

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# CocoaPods (not used, but just in case)
Pods/
Podfile.lock

# Carthage (not used, but just in case)
Carthage/Build/

# macOS junk
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Playgrounds (keep sources, ignore execution artifacts)
timeline.xctimeline
playground.xcworkspace

# Credentials / sensitive config
*.p12
*.cer
*.mobileprovision
